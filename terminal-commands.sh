# Stage all changes
git add .

# Commit the changes with a descriptive message
git commit -m "Fix font loading issues, update image URLs, and fix API route"

# Push to the remote repository (assuming you're on the main branch)
git push origin main